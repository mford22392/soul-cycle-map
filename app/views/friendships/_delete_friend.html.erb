<%= button_to "Remove friend", friendship_path(Friendship.find_by(:user_id => current_user.id, :friend_id => potential_friend.id)), remote: true, :method => :delete, form_class: "delete-friend", class: "btn btn-xs btn-info" %>